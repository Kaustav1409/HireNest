/**
 * Previously used to generate vercel.json at build time.
 * No longer needed — vercel.json is now committed as a static config.
 * Kept as a no-op for backwards compatibility.
 */
console.log("[hirenest] vercel.json is now static — no generation needed.");
