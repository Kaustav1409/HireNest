# Build output
target/
BOOT-INF/
*.class

# Local database & uploads (never commit user data)
data/
data_backup*/
uploads/

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# Logs & JVM crash dumps
*.log
hs_err_pid*.log
replay_pid*.log

# Decompile / temp rebuild (not source of truth)
decompiled-src/
target/jar-rebuild/
target/jar-patch/
target/ms-check*/
target/compile-out/
target/classes/

# Secrets (use env vars in production)
.env
.env.*

# Large tools (optional; remove line if you want cfr.jar in repo)
# tools/
